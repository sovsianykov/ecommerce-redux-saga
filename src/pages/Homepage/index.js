import React from 'react'
import Directory from "../../components/Directory";

const Homepage =( ) =>{
    return (
        <section>
         <Directory/>
        </section>

    );
}
export default Homepage